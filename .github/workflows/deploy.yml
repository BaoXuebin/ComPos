name: Build and Push to ACR

on:
  push:
    branches: [main]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Login to ACR
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.ACR_REGISTRY }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build backend image
        run: |
          docker build -f Dockerfile.backend \
            -t ${{ secrets.ACR_REGISTRY }}/${{ secrets.ACR_NAMESPACE }}/compos-backend:${{ github.sha }} \
            -t ${{ secrets.ACR_REGISTRY }}/${{ secrets.ACR_NAMESPACE }}/compos-backend:latest \
            .

      - name: Build web image
        run: |
          docker build -f Dockerfile.web \
            -t ${{ secrets.ACR_REGISTRY }}/${{ secrets.ACR_NAMESPACE }}/compos-web:${{ github.sha }} \
            -t ${{ secrets.ACR_REGISTRY }}/${{ secrets.ACR_NAMESPACE }}/compos-web:latest \
            .

      - name: Push images
        run: |
          docker push ${{ secrets.ACR_REGISTRY }}/${{ secrets.ACR_NAMESPACE }}/compos-backend:${{ github.sha }}
          docker push ${{ secrets.ACR_REGISTRY }}/${{ secrets.ACR_NAMESPACE }}/compos-backend:latest
          docker push ${{ secrets.ACR_REGISTRY }}/${{ secrets.ACR_NAMESPACE }}/compos-web:${{ github.sha }}
          docker push ${{ secrets.ACR_REGISTRY }}/${{ secrets.ACR_NAMESPACE }}/compos-web:latest
